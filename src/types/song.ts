export interface Song {
    id: string;
    name: string;
    artist_name: string;
    image_cover: string;
    duration: number;
}
export interface SongGenre{
    id: string;
    image_cover: string;
}
